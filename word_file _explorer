user_input = input("Enter Filename:".upper())
fname      = open(user_input)
for x in fname:
        pos  = x.find("######")
        if pos  != -1:
           pos1  = x[pos:].strip()
           print(pos1)
quit()
